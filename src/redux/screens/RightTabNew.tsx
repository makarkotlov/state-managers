import React from 'react';
import {ScrollView} from 'react-native';
import {NewItem5} from '../components/NewItem5';

export const RightTabNew = () => {
  return (
    <ScrollView>
      <NewItem5 />
    </ScrollView>
  );
};
